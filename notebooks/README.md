# Exploration notebooks

Use for exploratory analysis only. Move reusable logic to source modules. Clear outputs containing credentials, restricted data or large artifacts before committing; final evaluation must be reproducible outside a notebook.
