# Model cards

One card per released model: target, training sources/counts, exclusions, split, metrics, calibration, applicability, limitations, artifact URI/checksum and reproducible command. Model binaries do not belong here.
