# Human DILI model — Team B

Own data curation, features, training, evaluation and batch inference. Organise runtime code in src/ and reproducible settings in configs/. Store weights in local artifacts/models/ and publish metadata in model_cards/. Keep the trained and pretrained paths behind the same contract. No model is implemented yet.
