# Model configurations

Commit reproducible non-secret settings: dataset manifest version, seed, split policy, feature parameters, model parameters and calibration policy. Never encode a test-derived threshold as a default.
