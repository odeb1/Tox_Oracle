# Local generated artifacts

Everything here except this README is ignored by Git. Use models/ for weights, runs/ for outputs and cache/ for downloaded structures and service responses. Publish only reviewed shareable summaries to evaluation/reports/ or demo/examples/. This is not storage for sensitive subject-level data.
