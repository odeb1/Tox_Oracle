# Discovery tests

Adapter tests use an injected fake transport and write only temporary artifacts; they do not require credentials, network access, or a GPU.

Run from `discovery/`:

```bash
python3 -m unittest discover -s tests -v
```

Add shared-output contract tests when Team A's conventional toxicity comparator is selected and the discovery evidence is wrapped in the joint v2 envelope.
