# Evaluation

Reusable evaluation code belongs in src/; curated shareable model reports belong in reports/. Full generated runs belong under local artifacts/runs/. Report model/data versions, split, class counts, metric definitions and thresholds.
